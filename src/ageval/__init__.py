"""agent-eval-harness — measure whether a tool-using agent actually completes the task.

Scores task success, tool-call accuracy, step efficiency, and cost-per-task across a
benchmark of multi-step tasks with known-correct outcomes and reference tool traces.
"""

__version__ = "0.1.0"
